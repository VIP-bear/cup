spring:
  datasource:
    url: jdbc:mysql://182.92.159.2/cup?characterEncoding=utf-8&useSSL=false
    username: root
    password: 123456
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    # 打印sql语句
    show-sql: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        # 格式化sql语句
        format_sql: true
server:
  port: 8082